---
clusterName: "homelab"

plugins:
  enabled: true

opencost:
  ui:
    ingress:
      enabled: true
      ingressClassName: "nginx"
      hosts:
        - host: opencost.kube
          paths:
            - /
  customPricing:
    enabled: true
    costModel:
      description: India Pricing
      CPU: 1.25
      RAM: 0.50
      storage: 0.25
