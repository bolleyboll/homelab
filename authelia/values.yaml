---
ingress:
  enabled: false
  className: "nginx"

configMap:
  storage:
    local:
      enabled: true
  notifier:
    filesystem:
      enabled: true
  cookies:
    domain: "authelia.lan"
  telemetry:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
