---
ingress:
  enabled: true
  className: "nginx"

configMap:
  storage:
    local:
      enabled: true
  notifier:
    filesystem:
      enabled: true
  session:
    cookies:
      - domain: "authelia.lan"
  authentication_backend:
    ldap:
      enabled: true
      address: "ldap://lldap.lldap.svc:3890"
      base_dn: "dc=homelab,dc=local"
      user: admin
  telemetry:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
