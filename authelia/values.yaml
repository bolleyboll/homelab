---
ingress:
  enabled: false
  rulesOverride:
    - rule:
        - host: "authelia.lan"
  className: "nginx"

configMap:
  telemetry:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
