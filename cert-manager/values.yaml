---
global:
  logLevel: 1

crds:
  enabled: true

prometheus:
  enabled: true
  servicemonitor:
    enabled: true

extraObjects:
  - |
    apiVersion: cert-manager.io/v1
    kind: ClusterIssuer
    metadata:
      name: selfsigned-issuer
    spec:
      selfSigned: {}
