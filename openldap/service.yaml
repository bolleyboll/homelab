apiVersion: v1
kind: Service
metadata:
  labels:
    app: ldap
  name: ldap-service
spec:
  type: LoadBalancer
  ports:
    - port: 389
      targetPort: 389
      name: openldap
    - port: 636
      targetPort: 636
      name: openldap-secure
  selector:
    app: ldap
